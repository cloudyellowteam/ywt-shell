{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "SDK Debugger",
            "program": "${workspaceFolder}/sdk/sdk.sh",
            "args": [
                "usage"
            ]
        }
    ]
}